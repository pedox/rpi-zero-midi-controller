module.exports = {
  // Registed GPIO Pins
  buttons: [13, 26, 19],

  // Registed LED Pins
  leds: [23, 24],

  // OLED Config
  ledConfig: {
    width: 128,
    height: 32,
    microview: true,
  },
};
